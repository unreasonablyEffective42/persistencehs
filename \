module Main where

type V = Int

type Edge = (V, V)

type Tri = (V, V, V)

distance :: Point -> Point -> Double
distance (x1, y1, z1) (x2, y2, z2) = sqrt (dx * dx + dy * dy + dz * dz)
  where
    dx = x2 - x1
    dy = y2 - y1
    dz = z2 - z1

main :: IO ()
main = putStrLn "done"
